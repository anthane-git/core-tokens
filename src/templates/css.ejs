/**
* Do not edit directly
* Generated on <%= new Date().toUTCString() %>
*/

/* Color Tokens */
@media (prefers-color-scheme: light) {
:root {
<% _.each(allProperties, (prop) => {
  if(!prop.name.includes('light')) return %> --<%= prop.name.replace('light', 'color') %>:<%= prop.value %>;
<% }); %>
}
}

@media (prefers-color-scheme: dark) {
:root {
<% _.each(allProperties, (prop) => {
  if(!prop.name.includes('dark')) return %> --<%= prop.name.replace('-dark', 'color') %>:<%= prop.value %>;
<% }); %> }
}


/* Spacing Tokens */
:root {
<% _.each(allProperties, (prop) => {
  if(!prop.name.includes('spacing')) return %> --<%= prop.name %>: <%= prop.value %>px;
<% }); %> }

/* Text Style Tokens */
<% _.each(allProperties, (prop) => { if(!prop.value.hasOwnProperty('font')) return %> 
.<%= prop.name %> {
  font-family: "<%= prop.value.font.family %>", sans-serif;
  font-weight: <%= prop.value.font.weight %>;
  font-size: <%= prop.value.font.size %>px;
} <% }); %> 